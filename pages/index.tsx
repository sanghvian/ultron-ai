import Sidebar from '../components/Sidebar';
import { Layout } from 'antd';

const { Content } = Layout;

const HomePage = () => {
    return (<>
        <h1>Welcome to Ultron AI</h1>
        <p>This is the home page content.</p>
    </>
    );
};

export default HomePage;
